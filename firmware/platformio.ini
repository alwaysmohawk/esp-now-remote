; ESP-NOW Remote Control Project
; Two environments: remote (TX) and base_station (RX)

[platformio]
default_envs = remote, base_station
src_dir = src
lib_dir = lib

[env:remote]
platform = espressif32
board = seeed_xiao_esp32c3
framework = arduino
monitor_speed = 115200
build_flags =
    -D REMOTE
    ; -D DEBUG    ; uncomment to enable serial debug output
build_src_filter =
    +<remote/>
lib_deps =

[env:base_station]
platform = espressif32
board = lolin_s2_mini
framework = arduino
monitor_speed = 115200
build_flags =
    -D BASE_STATION
    ; -D DEBUG    ; uncomment to enable serial debug output
build_src_filter =
    +<base_station/>
lib_deps =
